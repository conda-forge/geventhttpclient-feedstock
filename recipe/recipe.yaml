# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json
schema_version: 1

context:
  version: "2.3.8"

package:
  name: geventhttpclient
  version: ${{ version }}

source:
  # pypi doesn't consistently have `tests``
  - url: https://pypi.org/packages/source/g/geventhttpclient/geventhttpclient-${{ version }}.tar.gz
    sha256: 92d8c8b14aa6f62467b28fc45e4a9cd661d96b176f88b515920bda31a9bf8cb1
    target_directory: dist
  - url: https://github.com/geventhttpclient/geventhttpclient/archive/refs/tags/${{ version }}.tar.gz
    sha256: 35a3e9273163dfaefe971248cad91a34d7e6e841c7f86586e1eed15ed44b31b8
    target_directory: src

build:
  number: 0
  script:
    - cd dist
    - ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation --disable-pip-version-check

requirements:
  build:
    - ${{ compiler("c") }}
    - ${{ stdlib("c") }}
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
  host:
    - pip
    - python
    - setuptools
  run:
    - brotli-python
    - certifi
    - gevent
    - python
    - urllib3

tests:
  - python:
      imports: geventhttpclient
  - files:
      recipe:
        - run_test.py
      source:
        - src/pyproject.toml
        - src/tests/
    requirements:
      run:
        - coverage
        - dpkt
        - pip
        - pytest
        - requests
    script:
      - python run_test.py

about:
  license: MIT
  license_file:
    - dist/LICENSE-MIT
    - dist/llhttp/LICENSE-MIT
  summary: A high performance, concurrent http client library for python with gevent
  homepage: https://github.com/geventhttpclient/geventhttpclient

extra:
  recipe-maintainers:
    - bollwyvl
